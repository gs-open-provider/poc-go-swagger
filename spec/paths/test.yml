get:
  responses:
    200:
      description: OK
      schema:
        $ref: './definitions/text-response.yml'
post:
  parameters:
    - name: test-obj
      in: body
      schema:
        $ref: './definitions/text-response.yml'
  responses:
    200:
      description: OK
      schema:
        $ref: './definitions/text-response.yml'
